ktor {
    deployment {
        host = "0.0.0.0"
        port = 9081
    }
    application {
        modules = [ com.muedsa.upscayl.ApplicationKt.module ]
    }

    redis = {
        host = ${REDIS_HOST}
        port = ${REDIS_PORT}
        password = ${REDIS_PASSWORD}
    }

    image {
        download = {
            limit = ${IMAGE_DOWNLOAD_LIMIT}
        }
        temp = {
            dir = ${IMAGE_TEMP_DIR}
        }
        upload {
            url = ${IMAGE_UPLOAD_URL}
            token = ${IMAGE_UPLOAD_TOKEN}
        }

        provider {
            host = ${IMAGE_PROVIDER_HOST}
            token = ${IMAGE_PROVIDER_TOKEN}
        }
    }

    upscayl {
        processPath = ${UPSCAYL_PATH}
        modelsDirPath = ${UPSCAYL_MODELS_DIR}
        libsDirPath = ""
        libsDirPath = ${?UPSCAYL_LIBS_DIR}
    }
}

